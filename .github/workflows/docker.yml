name: Docker series build
on: push

jobs:
  builds:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: docker build
      run: docker  build / /home/runner/work/docker-container/-t ganeshkrish/maven-web-application:1 .
 




